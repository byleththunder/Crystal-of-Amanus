using UnityEngine;
using System.Collections;
using TeamUtility.IO.Examples;

public class JoystickDetector : MonoBehaviour {
    bool use = false;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {

        //if(Input.GetJoystickNames().Length >0 && !use)
        //{
        //    use = true;
        //    GetComponent<GamepadToggle>().Toggle();
        //}else
        //{
        //    use = false;
        //    GetComponent<GamepadToggle>().Toggle();
        //}
        
	}
}
